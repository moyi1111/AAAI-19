Before running the code you should run the file named make_matlab64.m to creat the tool box.

And then you could run Mycode.

You could adjust the values of the parameters to get a better result.

You could use the initial parameter value for the dataset of cora directly.